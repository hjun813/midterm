# 🚀 중간고사 대비 플랫폼 사용 가이드

이 프로젝트는 시험 예상 문제 데이터를 활용하여 효율적으로 학습하고 실전 테스트를 수행할 수 있는 웹 플랫폼입니다.

---

## 📂 1. 문제 데이터 추가 방법

플랫폼은 `src/problem/` 폴더 내의 JSON 파일을 자동으로 읽어옵니다.

### 파일 명칭 규칙
파일 이름은 다음과 같은 형식을 권장합니다:
`과목명_번호_이름.json` (예: `cloud_5_Docker&Migration.json`)

### JSON 데이터 형식
각 파일은 아래와 같은 객체들의 배열로 구성되어야 합니다:

```json
[
  {
    "id": 1,
    "type": "multiple",
    "question": "문제 내용",
    "options": ["보기1", "보기2", "보기3", "보기4"],
    "answer": "정답 내용",
    "explanation": "해설 내용",
    "difficulty": "medium",
    "probability": "high"
  }
]
```

### 지원하는 문제 유형 (`type`)
- **`multiple`**: 객관식 (4지선다)
- **`ox`**: O/X 문제
- **`blank`**: 빈칸 채우기 (정답이 여러 개인 경우 `["답1", "답2"]` 형식 사용)
- **`short`**: 단답형 주관식
- **`essay`**: 서술형 (자동 채점 대신 해설과 모범 답안 비교)

---

## 🖥️ 2. 주요 기능 사용법

### 🏠 대시보드 (Dashboard)
- 등록된 모든 문제와 과목별 현황을 한눈에 확인합니다.
- **랜덤 섞기**: 상단의 '랜덤 섞기'를 체크하면 학습/테스트 시 문제 순서가 무작위로 변경됩니다.

### 📖 학습 모드 (Study Mode)
- 정답을 바로 확인하며 한 문제씩 집중적으로 학습합니다.
- 오답 시 '오답 노트'에 자동으로 기록됩니다.

### ✍️ 테스트 모드 (Exam Mode)
- 실제 시험처럼 타이머를 작동시키고 문제를 풉니다.
- 종료 후 최종 점수와 오답 항목을 리포트로 제공합니다.

### 📑 오답 노트 (Review Notes)
- 틀린 문제들만 모아서 다시 복습하고 마스터할 수 있습니다.

---

## 🛠️ 3. 개발 및 실행 환경

### 로컬 실행
```bash
npm run dev
```

### 프로덕션 빌드
```bash
npm run build
```

### 주의 사항
- JSON 파일 작성 시 문법 오류가 있으면 화면에 표시되지 않을 수 있습니다.
- 새로운 파일을 추가한 후에는 페이지를 새로고침하면 자동으로 반영됩니다.
